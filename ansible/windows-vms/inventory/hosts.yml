# ============================================================================
# Ansible Inventory (windows_vms)
# ============================================================================
# Auto-generated from Terraform state - DO NOT EDIT MANUALLY
# Regenerate with: cd ansible && make inventory-all
# ============================================================================

all:
  children:
    proxmox_hosts:
      children:
        windows_vms:
          hosts:
            dc01:
              ansible_host: 10.10.0.10
              ansible_user: Administrator
              ansible_ssh_private_key_file: /home/user/.ssh/dc01_id_ed25519
              ansible_connection: winrm
              ansible_port: 5985
              ansible_winrm_server_cert_validation: ignore
              ansible_winrm_transport: basic
            dc02:
              ansible_host: 10.10.0.11
              ansible_user: Administrator
              ansible_ssh_private_key_file: /home/user/.ssh/dc02_id_ed25519
              ansible_connection: winrm
              ansible_port: 5985
              ansible_winrm_server_cert_validation: ignore
              ansible_winrm_transport: basic

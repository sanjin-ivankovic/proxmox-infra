# ============================================================================
# Ansible .gitignore
# ============================================================================
# Ansible-specific ignore patterns following GitOps best practices
# ============================================================================

# Ansible cache and facts
.ansible/
.ansible_facts/
.ansible_facts.json

# Vault password files (NEVER commit these)
.vault_password
*.vault_pass
.vault_pass
~/.vault_password

# Generated inventory files (auto-generated from Terraform)
inventory/*-hosts.yml
inventory/*-hosts.ini
inventory/all-hosts.yml
inventory/bootstrap.yml
inventory/configure.yml
inventory/terraform.yml
inventory/k3s-hosts.ini

# Keep inventory templates and examples
!inventory/*.example
!inventory/*.template

# Retry files
*.retry

# Python cache (for Python scripts like merge-inventories.py)
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual environments (if any)
venv/
env/
ENV/
.venv/

# IDE (project-specific, root handles common)
.idea/
.vscode/settings.json

# Temporary files
*.tmp
*.temp
*.log

# Local environment file for Terraform HTTP backend credentials
.env

# Kubeconfig directories (generated by playbooks)
kubeconfig/
k3s/kubeconfig/
linux-vms/kubeconfig/

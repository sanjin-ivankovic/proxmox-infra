{
  // Fix any fixable errors
  "fix": true,
  // Ignore files referenced by .gitignore (only valid at root)
  "gitignore": true,
  // Configuration for markdownlint CLI and VS Code extension
  "ignores": [
    // Archived applications
    "_archive/**",
    // Sanitization reports (auto-generated)
    "tools/sanitize/reports/**",
    // Test coverage reports (auto-generated)
    "htmlcov/**",
    // License files (plain text, not markdown)
    "LICENSE"
  ]
}
